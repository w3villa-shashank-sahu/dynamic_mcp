# Weather Tools Configuration
# This file contains the tool registry configuration for MCP server

WEATHER_TOOLS = {
    "getWeather": {
        "function": get_weather,
        "description": "Get current weather information",
        "parameters": {
            "type": "object",
            "properties": {
                "location": {
                    "type": "string",
                    "description": "Location to get weather for (optional)"
                }
            }
        }
    },
    "getTime": {
        "function": get_time,
        "description": "Get current time information",
        "parameters": {
            "type": "object",
            "properties": {
                "timezone": {
                    "type": "string",
                    "description": "Timezone to get time for (optional)"
                }
            }
        }
    },
    
    
}